# Core PyQt GUI & Media Processing
PyQt5>=5.15.2
opencv-python>=4.8.0
mediapipe>=0.10.0
numpy>=1.22.0,<2.0.0

# Deep Learning Frameworks
#
# IMPORTANT: plain `pip install torch` pulls the CPU-only build, which makes
# Kokoro TTS roughly 20x slower (measured on an RTX 3050: 4.5s vs 0.21s before
# Helio speaks). On an NVIDIA GPU, install the CUDA build instead of these:
#
#   pip install torch==2.8.0+cu128 torchvision==0.23.0+cu128 torchaudio==2.8.0+cu128 \
#       --index-url https://download.pytorch.org/whl/cu128
#
# Keep torch/torchvision/torchaudio versions matched — a mismatched torchaudio
# fails to import with "WinError 127".
torch>=2.0.0
torchaudio>=2.0.0
torchvision>=0.15.0

# Wake Word & Speech-to-Text
openwakeword>=0.6.0
faster-whisper>=1.0.0
sounddevice>=0.4.6
soundfile>=0.12.0
silero-vad>=6.0.0

# Text-to-Speech (TTS)
kokoro>=0.7.16
espeakng-loader>=0.2.4

# Face ID / Face Detection fallback
facenet-pytorch>=2.5.0

# General Utilities
requests>=2.31.0
scipy>=1.10.0
rapidfuzz>=3.0.0
psutil>=5.9.0

# Web Search
ddgs>=9.0.0

# Windows Shortcut (.lnk) Resolution
pylnk3>=0.4.3

# Document Readers (PDF, DOCX, PPTX, XLSX)
PyPDF2>=3.0.0
python-docx>=1.1.0
python-pptx>=0.6.23
openpyxl>=3.1.0

# Screen Vision ("what's on my screen?") — capture only; the reading is done
# by VISION_MODEL over Ollama. Already pulled in transitively, declared here
# because src/tools/vision/screen_vision.py imports it directly.
pillow>=10.0.0

# Document retrieval (the SETUP/FILES 'study this' capability).
# all-MiniLM-L6-v2 is ~90MB and runs on CPU by design - the 4GB card is
# already carrying Kokoro and Whisper.
sentence-transformers>=3.0.0

# Web page extraction for search_and_read / read_webpage. lxml only; no
# readability or trafilatura needed for article-shaped pages.
lxml>=5.0.0
